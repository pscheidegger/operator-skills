# PDF Ingestion Example

Source:

- PDF

Parser:

```yaml
parser: docling
```

Decision:

```yaml
ingestion_decision: pass-to-knowledge-extraction
```

Workflow:

PDF → Markdown → Knowledge Extraction